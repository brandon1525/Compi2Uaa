program {
	int x, y;
	float x1, y1;
	bool x2, y2;

	if( ( x + 20 ) * 4 + ( y * 3 ) / ( x1 + y2 ) ) {
		x := y *23;
	} else {
		if( x2 = 1 ) {
			y := x +21;
		} else {
			y := x +20;
		} fi
	}

	while( x1 * x + x2 * ( y1 - y2 ) > 0 ) {
		y2 := 1;
		do {
			x1 := x1 +1;
			read x1;
		} 
	}
}